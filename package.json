{
  "name": "whatsapp-contract-bot",
  "version": "1.0.0",
  "description": "WhatsApp bot to create contracts, generate PDFs, collect payments, send reminders, and audit events.",
  "main": "src/server.js",
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest --runInBand",
    "lint": "echo 'No linter configured'",
    "health": "node -e \"require('http').get('http://localhost:'+(process.env.PORT||3000)+'/health',res=>{console.log('status',res.statusCode);process.exit(res.statusCode===200?0:1)})\""
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mercadopago": "^1.6.9",
    "node-cron": "^3.0.3",
    "pg": "^8.12.0",
    "pino": "^9.4.0",
    "twilio": "^5.3.3",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.7",
    "pino-pretty": "^11.2.2",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "roots": [
      "<rootDir>/tests"
    ]
  }
}
